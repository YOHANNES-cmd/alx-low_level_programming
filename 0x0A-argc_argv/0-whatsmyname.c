#include <stdio.h>
/**
 * main - entry point
 * @argc: arg count
 * @argv: array of pointers to arg string
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
